allprojects {
	ext {
		shortVersion = '4.1.0-rc4+xep0080'
		isSnapshot = false
		jxmppVersion = '0.5.0-alpha2'
		smackMinAndroidSdk = 8

		// The baseline version to check the API's binary
		// compatibility against with Clirr.
		clirrBaseline = '4.1.0-rc2'
		clirrFailOnErrors = false
	}
}
